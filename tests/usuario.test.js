test('Debe crear un usuario', () => {
    
});

test('No debe crear un usuario ya registrado', () => {
    
});

test('No debe crear un usuario sin todos los campos', () => {
    
});

test('No debe crear un usuario con role no existente', () => {
    
});